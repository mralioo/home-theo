# --- Core (always needed, runs the deterministic demo) ---
# fastapi and pydantic floors are driven by google-adk 2.0.0 constraints
fastapi>=0.124.1,<1
uvicorn[standard]==0.34.0
pydantic>=2.12,<3
sse-starlette==2.1.3
pydantic-settings==2.6.1

# --- LLM / agentic mode (only needed when USE_LLM=1) ---
# Pin google-adk! It ships breaking changes on a ~bi-weekly cadence.
google-adk[dev]==2.0.0
python-multipart>=0.0.9
litellm>=1.63.0

# --- Dev / test ---
httpx>=0.27,<1
pytest==8.3.4
